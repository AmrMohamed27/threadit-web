query Me {
  me {
    errors {
      ...FullErrorField
    }
    user {
      ...FullUser
    }
  }
}

query CheckToken($options: CheckTokenInput!) {
  checkToken(options: $options) {
    errors {
      ...FullErrorField
    }
    success
  }
}
